---
// Reusable component for page titles with background image
import heroImage from '/public/images/hero-background.jpg'; // Import the image directly

interface Props {
	title: string;
	subtitle?: string; // Optional subtitle
}

const { title, subtitle } = Astro.props;
// Eksplicitno dodajemo base prefiks - VIŠE NIJE POTREBNO ZA SLIKU
const base = import.meta.env.BASE_URL; // Get base URL once (still needed for other links potentially)
// Removed sectionStyle definition
// Removed textShadow variable as it's now a Tailwind class
// const textShadow = 'text-shadow: 0 1px 3px rgba(0,0,0,0.4);'; 
---

{/* Removed inline style and background classes */}
<section 
  class="relative py-16 md:py-24 text-white" 
>
  <div class="relative z-10 container mx-auto px-6 text-center">
    <h1 class="text-4xl md:text-5xl font-bold text-white mb-2 text-shadow-sm">{title}</h1> {/* Added text-shadow-sm class */}
    {subtitle && <p class="text-lg md:text-xl text-gray-200 max-w-2xl mx-auto text-shadow-sm">{subtitle}</p>} {/* Added text-shadow-sm class */}
  </div>
</section>
